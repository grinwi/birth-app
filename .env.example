# Birthdays App - Environment Variables (example)
# Copy this file to .env.local for local development (Next.js + vercel dev),
# and set the same values in Vercel Project Settings → Environment Variables.

# --- Primary datastore: Vercel Blob (JSON) ---
# Use the actual token VALUE (the long secret string), not just the token name.
# Store ID is not required separately — the Base URL already includes the bucket identifier.
BLOB_BASE_URL=https://your-bucket-id.public.blob.vercel-storage.com
BLOB_READ_WRITE_TOKEN=PASTE_YOUR_READ_WRITE_TOKEN_VALUE_HERE
# JSON object key (filename) inside the blob store
BLOB_JSON_KEY=birthdays.json

# --- GitHub JSON backup & PRs ---
# Fine-grained PAT with repository access to only the target repo and minimum permissions:
# Contents: Read/Write, Pull Requests: Read/Write (Metadata Read is automatic).
GITHUB_TOKEN=PASTE_YOUR_GITHUB_FINE_GRAINED_PAT_HERE
GITHUB_REPO_OWNER=grinwi
GITHUB_REPO=birth-app
GITHUB_BRANCH=main
# JSON snapshot path in the repository (canonical backup)
GITHUB_JSON_FILE_PATH=birthdays.json

# --- Auth / Admin bootstrap ---
# Long random secret used to sign JWT (HS256).
AUTH_SECRET=PASTE_A_LONG_RANDOM_SECRET_HERE
# One-time bootstrap password for username "admin"; create first admin by logging in once.
ADMIN_INITIAL_PASSWORD=PASTE_INITIAL_ADMIN_PASSWORD_HERE

# --- Optional (JWT max age; default 1209600 = 14 days) ---
AUTH_TOKEN_TTL_SECONDS=1209600

# --- Optional (Bootstrap/Recovery sync) ---
# Shared secret used to authorize /api-py/sync (bootstrap data from GitHub JSON into Blob).
# Set a long random value. Required only if you use the sync endpoint.
BOOTSTRAP_TOKEN=
